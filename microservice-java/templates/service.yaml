apiVersion: v1
kind: Service
metadata:
  name: {{ include "microservice-java.fullname" . }}
  labels:
    {{- include "microservice-java.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "microservice-java.selectorLabels" . | nindent 4 }}
